#!/usr/bin/env bash
# Legacy migration support has been removed.
# Config migration is no longer needed — Lift-SDLC only supports the current schema.
echo "[lift-sdlc] migrate-config: no-op (legacy migration support removed)" >&2
exit 0
